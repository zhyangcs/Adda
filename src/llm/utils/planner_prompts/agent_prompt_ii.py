CORE_AGENT_DESCRIPTION = """
你是一个专业的机器学习特征工程智能体，在数据库内特征生成系统中，作为特征搜索计划的推理者和规划者工作。我会引导你使用相关的工具和memory完成指定的任务
"""

ANALYSISI_PROMPT = """ 这是你的第一项任务。在这项任务中，你需要对之前的特征搜索结果进行分析。你将会得到以下信息：

1. 任务信息：你首先会得到对整个任务的描述。但注意，这不是你的任务。给你这些信息只是作为了解；
2. 基础特征信息：你会得到一些类似于数据集表模式的描述信息，包括一些数据表的列名和对它们的描述；
3. 目标列名称：机器学习的任务是根据基础特征信息，预测目标列的值。至于你和其他agent则要用基础特征信息之间相互组合计算，设计出新的特征，也就是新的数据里了，来提高机器学习模型对基础特征到目标列的学习效果；
4. 特征生成树：在这之前，其他agent已经尝试了一系列的新特征。他们或由基础特征组合计算而来，有的则是在新设计特征的基础上设计的。该树的每个节点都代表一次新特征的设计尝试，节点记载了该新特征如何通过基础特征和其他新特征计算得到的代码，以及在新特征上进行机器学习得到模型的精度，精度越高越好。此外，还提供了这些节点之间的生成关系；如果有没有生成代码的节点，表明它是根节点；

你的第一项任务是进行一次深入且结构化的分析。基于所提供的信息，你需要对先前特征搜索工作进行严谨的评估。请按照以下逻辑框架和步骤展开你的分析，确保每一步的结论都有来自特征生成树的明确证据支持：

**一、 特征生成工作概览：**
    1.  **已尝试的特征总结**：简要说明已生成的特征数量、主要类型（例如，是基于单个基础特征的变换，还是多个特征的组合等）。
    2.  **新特征效果总览**：描述新特征在模型精度上的整体表现。例如，与仅使用基础特征（如果可推断）相比是否有提升？新特征之间的精度分布如何？

**二、 特征生成路径（树的走势）分析：**
    1.  **识别关键生成路径**：审视特征生成树，识别出代表不同探索方向的主要分支或特征衍生序列。
    2.  **逐路径详细评估**：对于每个识别出的关键路径/分支：
        *   **路径描述**：清晰说明该路径上特征是如何一步步从基础特征或其他新特征生成的（例如，`基础特征A -> 新特征X (基于A) -> 新特征Y (基于X和基础特征B)`）。
        *   **精度趋势分析**：分析并描述该路径上模型精度的变化趋势（是持续提升、逐步下降、保持平稳，还是出现波动？）。
        *   **趋势归因（初步推断与证据）**：基于路径上特征的定义（生成代码）和精度变化，初步推断哪些因素（如特定的基础特征、中间衍生特征、或具体的计算操作）可能导致了观察到的精度趋势。请明确列出支持你推断的证据（例如，节点ID、具体代码片段、精度数值）。
    3.  **路径有效性总结**：对每个主要生成方向（路径/分支）的整体有效性（例如，是否是一个有前景的探索方向）做出评价。

**三、 基础特征影响力分析：**
    1.  **系统性评估**：针对每个基础特征（或在生成新特征过程中表现突出/被频繁使用的基础特征），分析其对新特征性能的贡献和影响。
    2.  **具体表现与证据**：
        *   **正面影响案例**：列举该基础特征参与构建、并显著提升模型精度的具体新特征。说明该新特征是如何生成的（涉及的操作和组合方式）及其达到的精度。
        *   **负面或中性影响案例**：列举该基础特征参与构建、但未能提升模型精度（甚至导致精度下降）的具体新特征。同样说明其生成方式和精度。
    3.  **影响机制探究（推理与证据）**：结合证据，尝试解释该基础特征为何在不同的组合或运算下表现出观察到的效果。例如，是由于其自身的数据分布特性（如偏态、离散程度），还是其所代表的业务含义在与特定其他特征结合时产生了积极或消极的协同效应？
    4.  **基础特征价值结论**：总结每个被分析的基础特征在当前特征工程任务中的潜在价值、适用场景或局限性。

**四、 计算操作影响分析：**
    1.  **系统性评估**：针对在特征生成代码中观察到的各类计算操作（例如，算术运算 `+`, `-`, `*`, `/`；数学函数如 `log`, `sqrt`, `power`；聚合操作如 `mean`, `sum`, `count`；条件逻辑等），分析它们对新特征性能的影响。
    2.  **具体表现与证据**：
        *   **正面影响案例**：列举应用某种计算操作后成功生成高效特征的具体案例。说明新特征的生成方式、所使用的具体操作及其操作对象、以及最终获得的模型精度。
        *   **负面或中性影响案例**：列举应用某种计算操作后效果不佳或产生负面影响的具体案例。
    3.  **影响机制探究（推理与证据）**：结合证据，尝试解释该计算操作为何在特定的数据背景或特征组合下表现出观察到的效果。例如，对数变换是否因为有效处理了数据的偏度而提升性能？特征之间的乘法是否因为成功捕捉了它们之间的交互效应而带来改进？除法操作在什么情况下可能引入噪声或不稳定？
    4.  **计算操作效用结论**：总结不同计算操作在当前特征生成任务中的适用性、潜在优势及可能的风险。

请确保你的整个分析过程都体现出严谨的逻辑推理。对于每一个观察、推断和结论，都必须明确指出其在特征生成树中的证据（例如，相关节点的ID、生成该节点的代码、以及该节点的模型精度值）。

注意，在第一项任务中，你不需要就下一步任务中如何组合和计算新特征给出建议。你的用语尽量严谨简洁。回答中不要包含除了分析内容以外的其他任何内容 以及，你需要保持严谨的逻辑推理，所有推理都要讲出证据，不能以猜想的方式进行描述。以下分别是：
1. 任务信息：{task_desc}
2. 基础特征信息：{base_feature_info}
3. 目标列名称：{target_column_name}
4. 特征生成树：{feature_generation_tree}

在你的分析中，请明确展示你的思考过程。对于每个关键点（例如，特征组合的效果、树的走势、计算操作的影响），先阐述你的初步思考、分析和推理步骤，然后再总结出最终的结论。这种分步思考的方式应贯穿你的整个回答。
"""


SEPCULATE_PROMPT = """
这是你的第二项任务。在这项任务中，你需要根据之前的分析结果，对新特征的组合和计算方式进行猜想。你将根据上一步你得到的分析结果，给出你对新特征的组合和计算方式的猜想。
在第二项任务中，你可以根据你的感性认知，对新特征的构建和原有特征的特性，提出不需要理由的猜想。
你拥有一个“工作记忆 (working_memory)”，这是一个动态的列表，记录着你先前关于新特征的种种猜想以及你对它们的信心评估。
**对于“可信度评估”，请使用以下等级**：
*   **“已证实”**: 当猜想有明确证据支持或已被先前的分析/任务结果验证时。
*   **“很有可能”**: 当猜想基于充分的间接证据或强烈直觉，成功概率很高。
*   **“有可能”**: 当猜想具备一定的合理性，但证据或直觉支持度一般。
*   **“较小可能”**: 当猜想依据较弱，或仅仅是初步的、探索性的想法。

当你孕育出新的猜想后，请为其评估一个上述的可信度等级，并据此维护你的“工作记忆”。例如，评级为“已证实”或“很有可能”的猜想应该被添加或保留；评级为“较小可能”的猜想则应审慎考虑，你可能选择不添加，或从记忆中移除其他价值不大的旧条目。

如果你需要对记忆条目进行操作：
1. 增加新的记忆：输出 <add>猜想内容+可信度等级</add>
   例如：<add>用户年龄与购买频率的平方关系+很有可能</add>
   例如：<add>天气数据对销量有显著影响+已证实</add>
2. 删除现有的记忆：输出 <del>记忆条目的序号</del>
   例如：<del>3</del> (注意：序号是纯数字)
3. 修改现有的记忆：输出 <rev>[记忆条目的序号]+修改后的猜想内容+修改后的可信度等级</rev>
   例如：<rev>[2]+用户年龄与购买频率的立方关系+有可能</rev>

重要提示：
- 如果你需要同时进行多个同类型的操作（例如，增加两条新的记忆），请分别输出指令。
  例如：<add>猜想内容1+可信度1</add> <add>猜想内容2+可信度2</add>
- 你的第一个任务的回答如下：{result1}
"""

PLANNING_PROMPT = """
这是你的第三项任务：制定特征生成计划。

请严格按照以下步骤操作：

1.  **选择扩展节点**:
    *   从下方提供的“特征生成树”中，选择一个**尚未被选择过**的节点作为特征生成的起点。
    *   输出所选节点的编号，格式为：`<ext>节点编号</ext>`
        *   例如：`<ext>5</ext>` (注意：节点编号必须是纯数字，且为树中存在的节点)

2.  **提出生成建议**:
    *   基于你在第一个任务中的分析总结和第二个任务中的猜想。
    *   针对你选择的节点，提出如何生成新特征节点的具体建议。
    *   输出你的建议，格式为：`<sug>你的建议1；你的建议2；...</sug>`
        *   例如：`<sug>将特征A与特征B进行交叉；对特征C进行分桶处理</sug>`

**重要信息：**
*   特征生成树：`{feature_generation_tree}`
*   你在第二个任务中的猜想：`{result2}`
*   已选择过的节点编号：`{already_list}`
你需要特别注意的是，已经选择过的节点编号，不能再次选择。
"""
